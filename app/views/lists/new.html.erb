<%= simple_form_for(@list) do |f| %>
  <div class="container mt-3">
    <div class="form-inputs">
      <%= f.input :name %>
    </div>

    <div class="form-actions">
      <%= f.button :submit %>
    </div>
  </div>
<% end %>
